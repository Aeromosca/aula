import numpy as np
N = 4
matriz = np.zeros((N, N))

for l in range(len(matriz)):
    for c in range(len(matriz[l])):
        matriz[l][c] = int(input(f"Insira o valor {l}, {c} para a matriz: "))

matriz = 

print(matriz)